# 低管理度高可用度的任务调度

## 环境支持

应当支持单例以及 k8s集群模式。

### 单例

在单例中对 spring 应该表示友好，就是可以充分利用现有 spring 能力。

### k8s集群

1. k8s Job 当前执行者是无法发起的（即无权限也无能力），所以这里需要第三方 api 介入。
2. Pod标准, 只有环境变量传值才是唯一靠谱的方式

## 模式支持

- cron 调度式
- 一次性，就是正常退出就是完成(并不表示任务必须成功完成)

## 约定

worker 支持环境变量`JOB_SCHEDULER_SERVICE_URL`作为调度器地址
